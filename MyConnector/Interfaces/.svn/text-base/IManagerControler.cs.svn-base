using System;
using System.Collections.Generic;
using System.Text;

namespace MyConnector.Interfaces
{
    public interface IManagerControler
    {
        IAuteursManager getAuteurManager(IDALContainer container);
        ISearchManager getSearchManager(IDALContainer container);
        IMediasManager getMediaManager(IDALContainer container);
        //ILivresManager getMediaManager(IDALContainer container);
        //ICdsManager getCdManager(IDALContainer container);        
    }
}
